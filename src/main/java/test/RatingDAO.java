package test;

import java.io.File;
import java.io.IOException;

import org.codehaus.groovy.transform.trait.Traits.TraitBridge;
import org.lenskit.data.dao.*;
import org.lenskit.data.events.Event;
import org.lenskit.data.packed.BinaryRatingDAO;
import org.lenskit.util.io.ObjectStream;

public class RatingDAO {
////	ObjectStream<T> s = new EventCollectionDAO()
//	public void config() throws IOException {
////		BinaryRatingDAO ratingDAO = BinaryRatingDAO.open(new File("D:\\newdocres\\dataset\\rating.csv"));	
////		ObjectStream<Event> os = ratingDAO.streamEvents();
////		Object o = os.readObject();
////		System.out.println(o);
//		Iterable<Event> i =
//	}
//	public static void main(String[] args) throws IOException {
//		RatingDAO rd = new RatingDAO();
//		rd.config();
//	}
}
